# Next native PCCS experiment: global/local cycle agreement

Status: full source worker, cached-target worker, source calibration and evaluation implemented on 2026-09-17; awaiting remote smoke and launch validation. Previous native dense-cycle batches achieved +2.3164/+2.2920/+2.1214 Exo2Exo points. The completed fixed-pixel batch reached +2.3568 but its incremental context advantage and negative control did not establish reliable context contribution; same-bank O-MaMa is +3.3790. Continue the user's goal without calling it achieved.

Hypothesis: global DINOv3 patch descriptors may contain insufficient object detail for small/thin masks; regional scalar summaries cannot recover information already mixed with background. Test additional object-centered native DINOv3 views and use their cycle evidence inside PCCS. Do not introduce the O-MaMa projector, checkpoint, DINOv2 or an external mask matcher.

Important geometry correction: the actual original BackwardCorrespondenceMatcher preserves image aspect ratio, normalizing height to768 and rounding width to a16-pixel multiple. The earlier native_context.py comment saying square resizing is inaccurate; its executed mask code used actual feature-grid dimensions and therefore did not impose a square grid. Do not use aspect distortion as an explanation for earlier failures.

Views: source user mask and each existing target candidate define a square crop at1.5/2 times maximum bbox extent. Shift the crop inside image bounds, keep all foreground pixels, cap size to the shorter image side. If the full bbox cannot fit, or fewer than16 foreground pixels exist, skip local evidence and retain global evidence. Minimum side32. Record actual crop/effective scale; saturated identical crops are not independent confirmations.

Matched context control: at the same2× crop and resolution, retain foreground RGB exactly and replace background with ImageNet-mean RGB. This removes surrounding image content while retaining zoom; it is an intervention with a distribution shift, not proof of a particular neighbor-object mechanism. Compare separately trained equal-capacity confidence models on ordinary versus foreground-only views. Do not claim context gains if only zoom helps.

Use the existing native backward matcher's DINOv3 weights, image normalization and last-layer feature extraction; encode square views in batches up to4. Extract only after original SAM candidates are complete, so local evidence cannot directly modify them. Compute bidirectional mask transmission, chance-normalized cycle support and object similarity in local crop coordinates. Add these as native PCCS confidence inputs; original rule/quality constraints remain the fallback.

Data: reuse the exact48 fit takes/384pairs and16 calibration takes/128pairs and persisted take folds from pccs_dense_context_20260917. Source fit has676objects. Some earlier masks are cached, but256 fit pairs have no published raw mask bank; regenerate source candidates and save a new current bank if necessary, recording historical differences. All compared methods must share current candidates. Target uses the already-verified1094pair/20take bank and global evidence; local feature extraction and frozen decision replay suffice, avoiding another full SAM run.

Frozen search design before outcomes: seven groups (global-only, local1.5, local2, local2 foreground-only, global+local2, global+foreground-only, global+local1.5+local2), ridge10/100 or the existing 3-leaf/80-iteration HGB, thresholds .03/.05, and two independent admission controls (old cycle dominance; nonzero original soft-cycle >.001). This yields84 source configurations. Within each admission control all groups use exactly the same admission rule, independent of local evidence. Thus context versus foreground-only does not also change the candidate gate. Fit identical weighted candidate-minus-original IoU targets on source labels only, use the exact persisted takes, require positive OOF and disjoint-calibration gain and maximize calibration; ties choose first declaration. Freeze main and all same-model/same-threshold/same-gate feature controls before target. Original global/dominance source OOF and calibration must reproduce the earlier frozen study to1e-7. No significance or novelty claim based on search alone.

Stage order: source smoke8 pairs, standalone-cache smoke on those identical candidates, TRAIN384, calibration128, freeze, Exo2Exo1094 cached candidate evaluation, Exo2Ego existing512-pair965-object holdout with regenerated original inference. Exo2Ego images use the previously verified TEST image root, not the TRAIN-only directory. Full-file existence check precedes evaluation. Never load test results to fit a model. Both targets were observed in earlier research, so they are frozen benchmark comparisons, not fresh blind tests.

Source smoke verifies original model Capture OFF/ON, original outputs unchanged after ROI collection, strength0 fallback and batched versus single ROI features. Cache smoke loads only the original native DINOv3 backward matcher, compares its full parameter/buffer SHA to the actual PCCS matcher, copies its TF32/autocast contract, and compares all ROI scalar evidence against the in-pipeline smoke output. Before target reuse, all packed candidate bytes must match frozen hashes. Final selection runs through the actual native_select bridge and must equal offline policy replay; nulling GT metrics cannot change it. Source results retain current masks as private remote caches for reproducibility; masks/images are never published.

Foreground-only background uses rounded ImageNet-mean RGB[124,116,104]; foreground pixels and crop geometry remain unchanged. Invalid/empty/too-wide crops give zero local evidence with recorded validity and original PCCS fallback. The existing sampler and expert checkpoints do not change. Local encoders are additional passes, not free pooling: record number of views, measured collection time and peak GPU allocation. Timing covers all ablation views, not a deployment-optimized winning variant. No claim of lower cost before measurement. Segmentation experts/backbone are frozen; the small confidence calibrator uses TRAIN labels and is not fully training-free.

All data/code/logs in this folder; consolidate in ../pccs_gain_full_20260915/FINAL_REPORT.md and publish to existing branch pccs-omama-gain-20260916. Use one dedicated Qizhi job at a time; shared4090 belongs to other work. Supervise at0.8×valid ETA, min1minute, no cap; unknown/stage transitions5minutes. Existing dense-batch files and negative results must remain unchanged.
