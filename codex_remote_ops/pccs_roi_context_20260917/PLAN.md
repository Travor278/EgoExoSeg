# Next native PCCS experiment: global/local cycle agreement

Status: crop-view implementation and geometry tests prepared; full worker/training/evaluation are not yet implemented or submitted. This follows three completed native dense-cycle batches: +2.3164/+2.2920/+2.1214 Exo2Exo IoU points versus +3.3790 for the same-bank O-MaMa reference. Explicit neighborhood descriptors have not shown an incremental advantage. Continue the user's goal without calling it achieved.

Hypothesis: global DINOv3 patch descriptors may contain insufficient object detail for small/thin masks; regional scalar summaries cannot recover information already mixed with background. Test additional object-centered native DINOv3 views and use their cycle evidence inside PCCS. Do not introduce the O-MaMa projector, checkpoint, DINOv2 or an external mask matcher.

Important geometry correction: the actual original BackwardCorrespondenceMatcher preserves image aspect ratio, normalizing height to768 and rounding width to a16-pixel multiple. The earlier native_context.py comment saying square resizing is inaccurate; its executed mask code used actual feature-grid dimensions and therefore did not impose a square grid. Do not use aspect distortion as an explanation for earlier failures.

Views: source user mask and each existing target candidate define a square crop at1.5/2 times maximum bbox extent. Shift the crop inside image bounds, keep all foreground pixels, cap size to the shorter image side. If the full bbox cannot fit, or fewer than16 foreground pixels exist, skip local evidence and retain global evidence. Minimum side32. Record actual crop/effective scale; saturated identical crops are not independent confirmations.

Matched context control: at the same2× crop and resolution, retain foreground RGB exactly and replace background with ImageNet-mean RGB. This removes surrounding image content while retaining zoom; it is an intervention with a distribution shift, not proof of a particular neighbor-object mechanism. Compare separately trained equal-capacity confidence models on ordinary versus foreground-only views. Do not claim context gains if only zoom helps.

Use the existing native backward matcher's DINOv3 weights, image normalization and last-layer feature extraction; encode square views in batches up to4. Extract only after original SAM candidates are complete, so local evidence cannot directly modify them. Compute bidirectional mask transmission, chance-normalized cycle support and object similarity in local crop coordinates. Add these as native PCCS confidence inputs; original rule/quality constraints remain the fallback.

Data: reuse the exact48 fit takes/384pairs and16 calibration takes/128pairs and persisted take folds from pccs_dense_context_20260917. Source fit has676objects. Some earlier masks are cached, but256 fit pairs have no published raw mask bank; regenerate source candidates and save a new current bank if necessary, recording historical differences. All compared methods must share current candidates. Target uses the already-verified1094pair/20take bank and global evidence; local feature extraction and frozen decision replay suffice, avoiding another full SAM run.

Proposed families to preregister before outcomes: global-only control; local1.5; local2; local2 foreground-only; global+local context; global+local foreground-only. Use ridge10/100 and a small tree, .03/.05 confidence thresholds, exact persisted take-OOF checks; select only on OOF/calibration, freeze main and matched controls before target. Include region-view off/strength0 parity and precise mask/crop-coordinate checks. Report added encoder passes and runtime; no claim of lower cost before measurement.

All data/code/logs in this folder; consolidate in ../pccs_gain_full_20260915/FINAL_REPORT.md and publish to existing branch pccs-omama-gain-20260916. Use one dedicated Qizhi job at a time; shared4090 belongs to other work. Supervise at0.8×valid ETA, min1minute, no cap; unknown/stage transitions5minutes. Existing dense-batch files and negative results must remain unchanged.
