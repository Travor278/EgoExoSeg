# Frozen native PCCS ROI robustness check — 2026-09-17

Status: completed2026-09-17 05:31:09; job-3aa01e16-4967-4a6f-8e59-2a3215868277 successful, zero occupied nodes. Exo2Exo1094 and Exo2Ego512/965 both complete and independently verified; all models/thresholds remained frozen. Local20 secondary gains were+3.2772/+1.0748 points for this seed, versus+3.3101/+1.1958 for seed1. The source-selected local15 primary also gained in both directions and both seeds. This is robustness evidence on previously observed benchmarks, not a fresh blind test, statistical equivalence claim or new source search. No third seed is planned by this protocol.

Motivation: the source-selected local1.5 primary reached Exo2Exo41.7688; the declared local2 secondary reached42.3292, near frozen O-MaMa42.3980 on the same original candidate bank. The latter remains a secondary/exploratory finding in the first experiment. We must not relabel it as that experiment's original primary or optimize another threshold on those target labels.

Freeze the complete source-trained selection and matched controls from `../pccs_roi_context_20260917/selection.json`, including primary local15_ridge100_present_t0.05 and secondary local20_ridge100_present_t0.05. Keep checkpoints byte-identical. Compare with original PCCS and the earlier frozen cycle_ridge10_t0.03. No train/cal fitting, no model or threshold changes. Preserve zero-imputation/validity behavior exactly; missing ROI is not an unconditional routing lock. All results must include failed cases and zero IoUs.

The only intended inference change is the per-pair candidate-generation seed namespace: replace `candidate-quality-v1:` with `candidate-quality-v2:` in the existing SHA256(first4bytes,little-endian) modulo2**31-1 seed function. Keep dataset keys, annotation contents, source masks, corrected visual e19/fusion e20 weights, candidate samplers and masks/post-processing, metrics, DINOv3 precision and ROI geometry unchanged. Declare this namespace now, never retry another seed based on the gain.

One additional run: Exo2Exo full1094 pairs/20takes, then Exo2Ego existing512-pair965-object64-take cohort. Generate original candidates once per pair under the new seed; all method decisions and evaluations share that new bank. Historical masks are allowed and expected to differ, so record differences rather than asserting equality with seed1 or silently reusing its references. Validate source-encoder state, real native PCCS integration, offline replay, and unchanged candidate hashes after scoring. Do not compare new-seed native scores directly with old-seed O-MaMa scores.

For Exo2Exo, recompute the frozen O-MaMa canonical/native consensus reference on the new candidates in a separate reference process, with a historical witness check of score reproduction. Its network is solely an external baseline and cannot enter the native selector. Reuse scalar references only for cases whose three candidate hashes and original baseline routing are all identical. Use an old-bank witness even if no new-seed cases are unchanged. If reference reproduction fails, keep native results but mark the comparison incomplete until diagnosed; never substitute another bank's scores.

Report per-seed frame metrics and paired take-cluster confidence intervals. Do not count multiple seeds as independent new takes or pool them to inflate sample size. Native ROI versus strongest old native and same-bank O-MaMa comparisons remain distinct. This run evaluates robustness of frozen candidates already selected for study, not proof of statistical equivalence.

Implement and smoke-test the new-seed path before full evaluation. Keep all files under this directory and consolidate evidence in `../pccs_gain_full_20260915/FINAL_REPORT.md`. One dedicated authorized four-H100 job, no interference with shared4090 or other tasks. Supervision interval0.8×remaining stage ETA, rounded to nearest minute/min1; unknown or transition5min. Release nodes and record final state. Publish whitelisted code/scalars/logs/small models to the existing authorized branch; no raw images/masks/large features or credentials.

A later separate experiment may use natural-background mismatch interventions to distinguish semantic context from the distribution shift of mean-gray foreground-only crops. It is not part of this seed repeat and must not change its frozen choices.
