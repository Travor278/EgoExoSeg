# Exo→Exo expansion: provenance audit and frozen temporal stress test

User request: continue research and evaluate on more Exo2Exo samples. This experiment is an additional dataset test, not a change to the running full Exo2Ego job or its frozen parameters.

## What1094 actually means

Downloaded original dataset Jenk9/exo2exo-selected-EgoExo4D-pair-same-random@23b8f17b76aacbc15e269e03accc6a797bece202 contains547 human-approved pseudo-source masks, each paired with one synchronous and one random annotated target frame, hence1094. It covers20takes. README's79objects corresponds to79 distinct label strings; direct annotation audit finds96 take-scoped instance tracks and85 distinct source_object_id strings. Report these units separately. Source masks were generated with V2-SAM and SAM2 refinement then manually selected; target masks are annotated4K GT. Prior use of “full Exo2Exo” means this constructed1094-pair file, never the whole Ego-Exo4D corpus.

The overall Ego-Exo4D V2 release has5035takes, but not all camera/object/time combinations have segmentation GT. Existing local-platform corrected Exo2Ego full annotation has46515pairs over295takes; auditing every source path finds exactly one annotated exo camera per take, not a ready-made multi-exo GT pairing pool. Data volume alone cannot solve the missing second-view trusted prompt problem.

## Immediate expanded evaluation

Use only the previously approved source prompts and original target GT entries. For each(take,source_object_id,target_camera), collect its unique annotated target frames. Exclude every existing(source_prompt_id,target_image)pair; select up to16 new target times by equal quantiles, deterministically and before evaluating model outputs. This creates6534 additional pairs over19 of the existing20takes,80 take-scoped tracks,531 prompts and1000 target images. Combined with the untouched old1094, coverage is7628distinct pairs. There are **zero new takes, prompt masks or GT masks**. This is a temporal recombination stress test, not increased independent scene diversity, and cannot be advertised as such.

Each new pair retains source prompt annotation byte-equivalently and the target GT segmentation exactly from its original4K annotation record. Reject target annotation conflicts. No lexical category-only identity matching; use exact(take,source_object_id,camera). No manual or automatic selection based on PCCS/ROI success. Save exact source and GT provenance per pair; dataset hash is dd82c38fd5515c61a885476649015e66fe0b01d2db2ec4636d0079cf77aa7be5. Original file remains unchanged. Score the new6534alone; historical+new pooled summary may only be descriptive and must retain original/new separation and take-cluster statistics, never treat7628as independent scenes.

Models frozen: previous source-selected1.5×ROI primary, declared2×key secondary, seven matched controls and oldcycle. Good Exo2Ego Visual e19/Fusion e20 files and DINOv3 state verified by SHA; no refitting, threshold updates or new method selection. Candidate seed namespace v1 with newpair ID; original candidates generated once, all methods share them.8pair smoke checks capture OFF/ON,ROI does not mutate masks, fullMetric primary/local20/cycle vslabel-free replay. Full6534then independently recompute frozen O-MaMa reference on the same current candidate bank; historical old-bank witnesses check numerical reproduction before full run. Do not reuse old1094OMaMa metrics on newpairs. Preserve zeros and average objects withinpair then equalpair; report take-cluster95%bootstrap and19take limitation.

Resource plan: one separate dedicated4H100 expansion job,priority4,max360min,retention1min. The user's newer explicit request for an additional Exo2Exo test authorizes this bounded job alongside the existing Exo2Ego full verification; never use the ongoing full job's node/GPU/terminal or alter its files. Monitor both through the existing automation, nextcheck at0.8×the earliest meaningful remaining phase ETA, nearestminute,min1; unknown/transition5min. Both tasks must complete and release resources; new expansion completion does not replace full Exo2Ego seed1/2 commitments.

## Broader-scene expansion remains distinct

Asked user for original larger pseudo-source/selection directories. Need more take-scoped trusted exo prompts, ideally reviewed independently of ROI outcomes, to evaluate beyond20takes. Do not synthesize a second GT mask and call it ground truth. If automatic prompts are generated on newtakes, that must be a separately labeled noisy-prompt protocol, with audit/review and prompt-generation bias disclosed; avoid deriving prompts from the method under test and claiming independent generalization.

An initial audit of using existing540×960 Exo2Ego prompt annotations as extra target GT found identity/mask inconsistencies and many missing matching frames; its provisional722newpairs are NOT the main experiment and are not evaluated. Preserve annotation_identity_audit.json and expansion_manifest.json as diagnostic evidence; do not relax the consistency threshold just to increase sample count. The6534construction avoids this resolution/source change completely.

Research continues on UFM/RoMa v2 covisibility and uncertainty, and OmniGlue separated semantic/spatial evidence. Read ../pccs_method_research_20260917/RESEARCH.md. No external matcher is installed into PCCS; this larger sample run validates the existing frozen ROI method first. Save code/scalar/logs; rawdataset/predictionmasks remain private remote/local caches. Record results in the single master report with revised dataset-scope wording.
