# Frozen native PCCS ROI: full Exo→Ego and scientific controls

User request2026-09-17: full Exo2Ego validation with the best weights; a standalone method explanation; scientific ablations; confirmation of historical O-MaMa weight usage.

Full scope:46515 pairs,109253 objects,295 takes, two already-declared candidate seeds v1/v2. Run one four-H100 job per seed sequentially. The first job performs scientific controls, smoke1 and full1; the second runs smoke2/full2 after full1 coverage and release have been checked. Allow up to1440minutes per job; update supervision from actual remaining ETA×0.8, nearest minute,min1, no arbitrary upper bound. Unknown/stage change5minutes. No new training, model/threshold/selection changes, extra seeds or target tuning.

Freeze `pccs_roi_context_20260917/selection.json` SHA a913b51b9aa068706870adc070f8ec32e437776cb070c05150853ae896f043c3, including source-selected local15_ridge100_present_t0.05 and declared local20 secondary control; keep all seven matched feature controls and old cycle. The original source selection remains the primary historical experiment. For this requested validation, report both1.5/2x and all relevant matched controls without choosing a new winner from full-test scores.

Experts are corrected Visual e19 and Fusion e20, identical to HF revision8b8310e9ee13023cb8711c2417e18cc7361dc3aa. Verify bytes/SHA before inference. Use the full runtime from pccs_corrected_exoego_20260916 and check every image. That historical O-MaMa full run demonstrably used these same experts, complete46515pairs, baseline53.8997 and consensus56.6385. Its seed/candidate run differs; report historical numbers as separate runs, not a paired native-vs-O-MaMa test.

Original PCCS inference and all3 expert masks are produced once per pair. Reuse existing DINOv3 for extra local features, keep source/target masks and original measurements. Exact crop module/policy/encoder files remain byte-identical to ROI seed1; record source state/precision parity. Actual complete PCCSMetric must agree with offline decisions for1.5/2x/oldcycle; labels cleared in policy replay. Smoke additionally checks Capture OFF/ON, strength0 fallback, batch/single encoder equivalence and matched global/foreground-only routes. No full rawmask bank is needed; retain hashes, scalar evidence, all expert metrics and compact per-object choices for independent validation. This controls disk growth without affecting inference. Full coverage and frame-equal aggregation must retain zero IoUs, with10000 take-cluster bootstrap. Existing observed test takes are not an unseen blind benchmark; report historical512 subset and its complement separately if useful, never use either to tune parameters.

Scientific controls first, on fixed seed1 existing banks: Exo2Exo1094 and Exo2Ego512, after8-pair smoke. Freeze1.5/2x calibrators. Keep target image and every target candidate unchanged. Keep source foreground RGB, crop size, mask, coordinate convention and an8-original-pixel halo unchanged. Compare original source context to (a) natural donor background from the same source image, chosen deterministically among9 grid patches by least source-mask overlap then greatest spatial distance; (b) source crop rolled by half its side, then copy only pixels outside halo. No target GT is read. Record donor box/overlap, edit fraction and all evidence. If no editable background exists, intervention has zero strength and is retained, not excluded based on outcomes. Donor regions can contain other objects; this tests context compatibility, not named neighboring-object identity. The rolled control may carry displaced foreground patterns outside the protected region; disclose this imperfection rather than claiming a pure background randomization.

The real/sham arm must reproduce previous local feature scalars and decisions, encoder state must match, and all interventions preserve the protected foreground/halo bit-for-bit. Evaluate with the same frozen model rather than retraining on interventions; therefore performance loss could reflect unnatural seams or out-of-distribution content. Natural-donor and rolled controls reduce reliance on the previous constant-gray ablation but do not prove semantic causality. Report real minus intervention using paired take bootstrap, both scales and both directions, with no target-dependent filtering or calibration. All control comparisons are descriptive; no multiplicity-adjusted claim is made.

Artifacts under this folder; standalone METHOD.md with provenance and limitations, master report remains pccs_gain_full_20260915/FINAL_REPORT.md. Publish code/scalars/logs/small-model references and compact result artifacts to authorized pccs-omama-gain-20260916 branch; exclude images/masks/large tensors/dependencies/credentials/browser state/transport. Shared4090/other jobs remain untouched. On completion verify nodes0 and no own GPU processes. Full1 completion is not full double-seed completion: keep automation active until science, full1, full2, reports and publication are finished or a real external blocker requires user input.
