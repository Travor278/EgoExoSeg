# Exo→Exo：冻结 Exo→Ego 模型的直接迁移

**当前状态：登录已恢复，数据与源码已上传。4090因其他进程占用，在候选生成前安全退出；已提交4H100任务并通过32对预检，正在运行1094对全量迁移。任务ID见job_receipt.json。**

研究主目标为 Exo→Exo 涨点。Exo→Ego 全量 +3.6653 点只能证明前一方向有效，不代表 Exo→Exo 的性能。没有专用 Exo→Exo 权重时，借用 Exo→Ego 专家，但数据与报告方向仍为 Exo→Exo。

本次先做可审计的直接迁移：使用此前已准备的 1,094 对 Exo→Exo 标注和图像，冻结原 Exo→Ego V2-SAM 专家、公开 O-MaMa 头、已训练判断器及阈值0.03。比较原 PCCS、学习判断器、固定 O-MaMa 余弦优势0.05。每个阶段仅产生一次候选，全方法共用该bank，不按目标测试标签选择新门槛。

匹配几何保留此前canonical设定：源532×952、目标700×700、每边100px整框上下文。它把Exo→Ego几何先验一同迁移过去，必须作为限制报告；不能把迁移失败直接解释成上下文无效。早期测试曾观察过这个Exo→Exo基准，故不声称整个基准从未被观察。

`prepare.py` 在远端校验标注哈希、图像路径、take分组后创建独立代码与manifest。`launch.sh` 顺序运行32对预检和1094对全量；GPU预检遇到其他计算进程会拒绝启动，不抢占共享4090。所有新输出只写本目录。`read_monitor.js` 与 `transfer.py` 支持监督和结果取回。

Ego→Exo目前只有先前基线/context全量和O-MaMa小样本探索；尚未验证新学习判断器。它的PCCS baseline可能来自组合/阈值mask，当前门控特征只接受三种原始专家身份，因此需要先实现与检验相应适配，不能只替换命令中的方向字符串。

完整方法原理已补入兄弟目录 `pccs_gain_full_20260915/FINAL_REPORT.md`，并已同步远端。当前通过`launch_h100.sh`运行，按有效ETA的4/5安排监督；不要重复执行launch.sh或覆盖现有候选缓存。
