# 数据格式与用法

> 日期: 2026-07-13 · 全部字段/形状均在本地真实数据上实测确认
> 定位:V²-SAM→PAMI 的 robotic cross-view 数据源。

---

## 0. 一句话结构

数据是**两层叠加**,不是一个包:

```
DROID(底层)      : 同步多相机视频 + 本体感知   —— 无 mask
   └─ RoboInter(标注层): 在 DROID 之上做的逐帧标注(含 segmentation mask)
```

- **DROID**:每条 recording 有 3 路同步相机 `exterior_image_1_left` / `exterior_image_2_left`(2 外部)+ `wrist_image_left`(1 腕部)。原生 1280×720 立体 @15fps。官方无 mask。
- **RoboInter**:别人在 DROID 之上单独做的数据集,提供 **SAM2 生成 + 人工复核的逐帧 mask**,但**只在 2 个外部相机上,腕部无 mask**。

→ 所以:**exterior_1 ↔ exterior_2 的 exo↔exo pair,两侧 mask 都现成**(免标注);wrist↔exo 的腕部一侧要自己标。

---

## 1. 核心结构事实(最重要,决定怎么配对)

**RoboInter 的一个 "episode" = 一条 DROID recording 的一个外部相机。**

- 每个 episode 的 `episode_name = "{video_id}_{camera}"`,例如 `66378_exterior_image_1_left`。
- **同一条 recording(同一个 `video_id`)会出现成两个 RoboInter episode**:
  - `{video_id}_exterior_image_1_left`
  - `{video_id}_exterior_image_2_left`
  - 两者共享**同一段腕部视频**,但各自的"primary"是各自的那个外部相机。
- mask 挂在该 episode 的 `primary`(=它那路外部相机)上;两个 episode 各有一份 mask npz。

**这就是 exo↔exo pair 的来源**:同一个 `video_id`、两个外部相机、两份现成 mask、同一条时间轴(帧同步)。

```
video_id=66378 ─┬─ 66378_exterior_image_1_left  (primary=exterior_1, 有 mask npz)   ← view A
                └─ 66378_exterior_image_2_left  (primary=exterior_2, 有 mask npz)   ← view B
                    两个 episode 帧数相同、逐帧同步 → 同一物体、同一时刻、两个外部视角
```

---

## 2. RoboInter 仓库结构(`InternRobotics/RoboInter-Data`,HF 免 gate,~408GB)

| 路径 | 内容 | 备注 |
|---|---|---|
| `data/lerobot_droid_chunk-*/chunk-*/episode_*.parquet` | 每 episode 的**逐帧标注 + 本体感知**(LeRobot 格式) | DROID 共 152,986 episode |
| `videos/.../observation.images.{primary,wrist}/...mp4` | 打包的 **primary + wrist 两路视频**,180×320 @10fps | 低清;高清需回 DROID 原始 |
| `meta/lerobot_droid_anno/{info.json,episodes.jsonl}` | 特征定义 + episode 元数据(index/task/length) | |
| `VideoID_2_SegmentationNPZ.json` | **`episode_name` → mask npz 路径**的映射(null=无 mask) | 235,920 key,198,194 有 npz |
| `Annotation_raw/segmentation_npz.zip.{00,01,02}` | 全部 mask npz(**分卷 zip,~53GB**) | 见 §4 取法 |
| `All_Keys_of_Primary.json` | **Primary 质量档**的 key 列表(65,515) | 高质量子集,做 GT 优先用 |
| `Annotation_raw/{droid,rh20t}_annotation.pkl` | 原始标注 pickle(各 ~12–21GB) | 一般用不上 |

### 2.1 parquet 逐帧字段(实测 `episode_000000.parquet`)

一个 episode 的 parquet 是 `T` 行(T=帧数),关键列:

| 列 | 类型 | 含义 |
|---|---|---|
| `episode_name` | const | **`{video_id}_{camera}`** —— 与 mask key 同格式,配对靠它 |
| `camera_view` | const | 这个 episode 是哪路外部相机(`exterior_image_1_left`…) |
| `annotation.segmentation` | **空串** | mask **不在 parquet 里**,在外部 npz(见 §4) |
| `annotation.object_box` | **per-frame** | 目标物体的**逐帧 bbox**(如 `[[168,85],[231,132]]`),坐标在 180×320 系 |
| `annotation.origin_shape` | const `[320,180]` | RoboInter 处理分辨率 `[w,h]`;≠ DROID 原生 1280×720 |
| `annotation.time_clip` | const | 标注覆盖的帧区间,如 `[[0,239]]` |
| `annotation.{instruction_add,substask,primitive_skill}` | const | 语言/技能标注 |
| `annotation.{trace,gripper_box,contact_points,affordance_box,contact_frame,state_affordance}` | 多为 per-frame | 其它 10+ 类中间表征 |
| `Q_annotation.{...}` | const | 每类标注的**质量档**:`"Primary"`(最好)/`"Secondary"`;做 GT 筛 Primary |
| `frame_index / timestamp / episode_index` | per-frame | LeRobot 标准帧索引 |

> 注意:`annotation.object_box` 提供了**逐帧物体框**(免费),即使不下 mask,也能先用框做粗筛/可见性判断。

### 2.2 mask npz 格式(实测 `66378_exterior_image_1_left.npz`)

```python
z = np.load("66378_exterior_image_1_left.npz")   # 外层 zip 里 method=0 直存,~130KB
z.files                # -> ['masks']
z["masks"].shape       # -> (1, T, 1, 180, 320)     T=该 episode 帧数
z["masks"].dtype       # -> float64,取值 {0.0, 1.0} 即二值 mask
# 第 t 帧的物体 mask:
mask_t = z["masks"][0, t, 0]        # (180, 320) 二值,与 primary 视频逐帧对齐
```

- **单物体**(第 0 维、第 2 维都是 1):RoboInter 每 episode 跟踪一个 task 目标物体。
- **逐帧**:T 帧,与该 episode 的 primary 视频帧一一对应。
- **分辨率 180×320**,与 LeRobot 视频一致;要 DROID 原生高清需上采样或回原视频重跑 SAM2 refine。

本仓库已存一个参考样本:`RoboInter-Data/sample_npz/66378_exterior_image_1_left.npz`。

---

## 3. DROID 底层视频怎么取

RoboInter 的 primary/wrist 是 **180×320 低清**。要高清或第三路相机,回 DROID:

- **样本调试**:`lerobot/droid_100`(HF 免 gate,2GB)—— 有 `exterior_image_1_left` / `exterior_image_2_left` / `wrist_image_left` 三路 mp4 + parquet(见本仓库 `DROID/`)。
- **全量**:GCS `gs://gresearch/robotics`(`droid` RLDS 1.7TB / `droid_raw` 立体 8.7TB)。
- **video_id ↔ DROID recording**:RoboInter 的 `video_id`(如 66378)是 OXE_DROID 的 recording 序号;回原始高清帧时用它对齐(npz 路径 `/OXE_DROID/data/ann_human/0/sam_mask/{episode_name}.npz` 保留了溯源结构)。

---

## 4. 只取需要的 mask(不下 53GB)

mask 是分卷 zip `segmentation_npz.zip.{00,01,02}`。**两种取法:**

**A. 整卷下载后本地解压**(要 53GB 磁盘,一次性)。

**B. 按需 range 抽取(推荐做 pilot)**:zip 把每个文件的数据紧跟在其 local header 之后、顺序排列,可以只下开头若干 MB 抽出前几个 npz(本仓库就是这么取到样本的):

```python
import urllib.request, struct, io, numpy as np
url = "https://huggingface.co/datasets/InternRobotics/RoboInter-Data/resolve/main/Annotation_raw/segmentation_npz.zip.00"
buf = urllib.request.urlopen(urllib.request.Request(
    url, headers={"Range": "bytes=0-26214399"})).read()      # 前 25MB
assert buf[:4] == b'PK\x03\x04'
_,_,method,_,_,_,csize,_,nlen,elen = struct.unpack("<HHHHHIIIHH", buf[4:30])
name = buf[30:30+nlen].decode()                              # 第 1 个 npz 名
raw  = buf[30+nlen+elen : 30+nlen+elen+csize]                # method=0 直存
masks = np.load(io.BytesIO(raw))["masks"]                    # (1,T,1,180,320)
```
> 局限:zip 内文件是**顺序排列**、无按名随机访问;要精确取某个 `video_id` 的 npz,得顺序扫到它,或整卷下。做 hundred-level pilot 时,更省事的是**整卷下 `.00`(21GB)覆盖一批 video_id**,或直接下全量。

---

## 5. 怎么用 —— 构建 exo↔exo benchmark(路径 A,完全免标注)

对齐计划书 0708 的 task 定义:**query 1 帧 → target 所有帧**,json 格式对齐 ego2exo/exo2ego/HANDAL。

### 步骤

1. **筛候选 video_id**:遍历 `VideoID_2_SegmentationNPZ.json`,取 `{vid}_exterior_image_1_left` 与 `{vid}_exterior_image_2_left` **两路都非 null** 的 vid → 全量 **56,566 对**;落在 `val_video.json` 的 **1,678 对**;要最高质量再交 `All_Keys_of_Primary.json`。
2. **取两路 mask**:
   - view A: `{vid}_exterior_image_1_left.npz` → `masks[0,:,0]`(T×180×320)
   - view B: `{vid}_exterior_image_2_left.npz` → `masks[0,:,0]`
   - 两路帧同步(同一 recording 时间轴)。
3. **构 query/target**:
   - query = view A 里**随机抽 1 帧** t_q(mask 非空的帧)+ 该帧 mask;
   - target = view B 的**所有帧**(每帧要预测同物体 mask,GT = view B 该帧 mask);
   - **双向**再做一遍 B→A。
4. **写 json**(对齐 V²-SAM 推理格式):把 1 个 query 复制 N 份去检索 target 的 N 帧(参考 exo2exo 生成脚本)。
5. **可视化抽查** → 回 DROID 高清映射(可选)→ 跑 V²-SAM / ObjectRelator ZSL。

### 可跑的最小示例(本地已验证组件)

```python
import json, numpy as np

m = json.load(open("RoboInter-Data/VideoID_2_SegmentationNPZ.json"))
# 1) 找两路都有 mask 的 recording
def vid_of(key): return key.rsplit("_exterior_image_", 1)[0]
have = {}
for k, v in m.items():
    if "exterior_image_" in k and v:
        have.setdefault(vid_of(k), set()).add(k.split("_exterior_image_")[1][0])  # '1' or '2'
pairs = [vid for vid, s in have.items() if {"1", "2"} <= s]     # 两路都有
print(len(pairs), "exo-exo recordings")   # ~= 56,566(全量);本地样本子集会更少

# 2) 对一个 recording 取两路 mask,构 query->target
def load_masks(vid, cam):   # cam in {1,2}
    npz = f".../{vid}_exterior_image_{cam}_left.npz"     # 从 zip 抽或整卷解压后
    return np.load(npz)["masks"][0, :, 0]                # (T,180,320) 二值

A = load_masks(pairs[0], 1); B = load_masks(pairs[0], 2)
t_q = int(np.random.choice([t for t in range(len(A)) if A[t].any()]))  # A 里有物体的帧
sample = {
    "query":  {"view": "exterior_1", "frame": t_q, "mask": A[t_q]},     # 1 帧 + mask
    "target": {"view": "exterior_2", "frames": list(range(len(B))),     # 所有帧
               "gt_masks": B},                                          # 逐帧 GT
}
```

---

## 6. wrist↔exo(ego↔exo 同构,路径 B)

RoboInter **腕部无 mask**。若要做 wrist↔exo:
- query 侧用**外部相机现成 mask**(exterior_1 或 2);
- target 侧(wrist)**自建 mask**:对 wrist 视频跑 SAM2 视频传播 + 人工复核(见主报告 §6 的 auto-mask 流程);
- 工作量约为全自建的一半(query 侧免了)。

---

## 7. 必记 caveat

1. **腕部零 mask**:免标注只在 exo↔exo;wrist↔exo 要自建(§6)。
2. **低清 180×320**:交付质量需回 DROID 原生高清上采样 / SAM2 refine。
3. **跨视角物体一致性待抽查**:RoboInter 对 exterior_1 与 exterior_2 是**各自独立** SAM2 跟踪的,虽同属一条 recording 的同一 task 目标,但两路 mask 是否咬住**同一物体实例**需可视化 spot-check(尤其多相似物体场景)。
4. **质量档**:做 GT 优先用 `All_Keys_of_Primary.json`(Primary),`Secondary` 谨慎。
5. **帧同步 vs 时序错位**:exterior_1/2 是**逐帧同步**的(适合标准 cross-view);若要做计划书里的 temporal-misaligned split,再另从 query 下采样。

---

## 附:一步到位的核对命令

```powershell
# parquet schema + 一行样本
python -c "import pyarrow.parquet as pq; t=pq.read_table(r'RoboInter-Data/data/lerobot_droid_chunk-000/chunk-000/episode_000000.parquet'); print(t.column_names)"
# mask 形状
python -c "import numpy as np; print(np.load(r'RoboInter-Data/sample_npz/66378_exterior_image_1_left.npz')['masks'].shape)"
```

---
---

# 二、RH20T(次选)—— 数据格式与用法

> 官网 <https://rh20t.github.io/> · API <https://github.com/rh20t/rh20t_api> · 日期 2026-07-13,RoboInter 侧数字均本地实测。
> **RH20T 相对 DROID 的独有价值**:同一物体有 **8–10 路全局相机 + 全部标定到基座系** → (a) **exo↔exo 产能全场最高**;(b) **唯一能按视角夹角分桶做 geometric-ambiguity 梯度分析**(计划书 §2.3 的差异化卖点)。

## R0. 两条使用路线(先看这个)

| 路线 | 用什么 | 给什么 | 适用 |
|---|---|---|---|
| **① 经 RoboInter(推荐起步)** | RoboInter 的 RH20T 标注层 | **现成 mask,且同一 recording 多路全局相机都有 mask** → 直接组 exo↔exo | 快速起步、和 DROID 同一套代码 |
| **② 用官方 RH20T 外参(独有卖点)** | 官方 `calib/` 的基座系外参 | 任意两路全局相机的**相对夹角** → 按角度分桶 | geometric-ambiguity 梯度分析 |

两条路线**同一套 recording**,靠 RoboInter 的 `episode_name` 与官方目录名直接对齐(见 R3)。

## R1. 路线① —— 经 RoboInter(和 DROID 同一套 schema,但 exo↔exo 更强)

RoboInter 的 RH20T 部分与 DROID 部分**完全同构**(见 §2:primary+wrist 视频 + 13 类逐帧标注 + Q 质量档 + 外部 mask npz),差别只在命名与分辨率:

| 项 | DROID | **RH20T** |
|---|---|---|
| `episode_name` | `{video_id}_exterior_image_{1,2}_left` | **`RH20T_cfg{C}_task_{T}_user_{U}_scene_{S}_cfg_{C2}_{serial}`** |
| `camera_view` | `exterior_image_1_left` | **相机 `{serial}`**(如 `035622060973`) |
| `annotation.origin_shape` | `[320,180]` | **`[640,360]`** |
| 每条 recording 的外部相机数 | **固定 2**(exterior_1/2) | **8–10 路全局相机**(RoboInter 实测每 recording 有 mask 的多在 **4–8 路**) |
| mask npz | `masks` (1,T,1,180,320) | 同结构 `masks` (1,T,1,H,W);LeRobot 视频 180×320、origin `[640,360]`,**具体 H×W 下载后确认**(与 DROID 同理约 180×320) |

**exo↔exo 产能(本地实测 `VideoID_2_SegmentationNPZ.json` RH20T 部分):**

- RH20T mask key **82,920**,有 npz **69,328**;
- 有 mask 的 recording **12,191** 条,其中 **11,987 条有 ≥2 路 masked 全局相机**(可直接组 exo↔exo);
- 每 recording 的 masked 全局相机数分布:`{1:204, 2:385, 3:663, 4:1280, 5:2327, 6:3322, 7:2402, 8:1608}` → **多数是 5–8 路**;
- 一条 8 路的 recording → **C(8,2)=28 个 exo↔exo pair**,全场产能最高(DROID 每 recording 只有 1 对)。

**组 exo↔exo(和 DROID 同理,但可多路两两组合):**

```python
import json, re, collections
m = json.load(open("RoboInter-Data/VideoID_2_SegmentationNPZ.json"))
pat = re.compile(r'(RH20T_cfg\d+_task_\d+_user_\d+_scene_\d+_cfg_\d+)_(.+)$')
rec = collections.defaultdict(set)                       # recording -> {serial,...}
for k, v in m.items():
    if v and (mm := pat.match(k)):
        rec[mm.group(1)].add(mm.group(2))
# 每个 rec 里任取两路 serial 就是一对 exo↔exo;两路 mask:
#   {rec}_{serialA}.npz 与 {rec}_{serialB}.npz,各 masks[0,:,0]
multi = {r: s for r, s in rec.items() if len(s) >= 2}    # ~11,987 条
```
> query 1 帧 → target 所有帧的构造、写 V²-SAM json、双向,全部同 §5;只是可用 pair 更多、可挑角度(见 R4)。

## R2. 路线② —— 官方 RH20T 数据结构(做角度分桶用)

官方发布(Google Drive / 百度云,按 cfg 分):

```
RH20T/
├── RH20T_cfg1/ ... RH20T_cfg7/          # 7 种机器人配置
│   ├── calib/                            # 内参 + 外参(Aruco 标定,全部到机器人基座系)
│   ├── task_XXXX_user_XXXX_scene_XXXX_cfg_XXXX/       # 机器人 recording
│   │   ├── cam_{serial}/                 # 每路相机一个文件夹(8–10 全局 + 1–2 in-hand)
│   │   │   ├── color.mp4                 # 1280×720@10Hz(降采样版 640×360 / 320×180)
│   │   │   ├── depth.mp4                 # 深度(可选)
│   │   │   └── timestamps.npy            # 帧时间戳(用于跨相机对齐/抽帧)
│   │   ├── tcp_base.npy                  # 夹爪位姿(基座系,xyz+quat)
│   │   ├── force_torque_base.npy / high_freq_data.npy
│   │   └── metadata.json                 # 任务评分、标定质量(0–5)、场景时间戳
│   └── task_..._human/                   # 对应人类演示(等量,可做 human↔robot)
```

- **相机**:全部以 `cam_{serial}` 命名;**哪几路是 in-hand 要查 API/metadata**(命名上不区分全局/腕部)。
- **标定**:`calib/` 提供每路相机的内参 + 外参(到基座系)→ **这是路线②的关键**,可直接算任意两路相机的相对位姿/夹角。
- **下载**:三种分辨率——640×360(通用,~178–228GB/cfg)、320×180(深度无损)、1280×720 原始(40TB,不建议);GDrive + 百度云。
- **license 分段**:RH20T-C(scene 0001–0005)= CC BY-SA 4.0(可商用);RH20T-NC(scene 0006–0010)= CC BY-NC 4.0。**测试集尽量落 RH20T-C**。
- **官方 API**:`rh20t_api`(github.com/rh20t/rh20t_api)—— 用 `timestamps.npy` 从 mp4 抽帧、加载标定。用它读数据最省事。

## R3. RoboInter ↔ 官方 的对齐(把两条路线接起来)

RoboInter 的 `episode_name` 直接解析出官方目录:

```
RoboInter key : RH20T_cfg1_task_0001_user_0001_scene_0001_cfg_0001_035622060973
                 │      │         │          │           │            └ 相机 serial → 官方 cam_035622060973/
                 │      └─ 官方 recording 文件夹 task_0001_user_0001_scene_0001_cfg_0001/
                 └─ 官方顶层 RH20T_cfg1/ + 同目录 calib/
```

→ 有了 serial + cfg/scene,就能在官方 `calib/` 里查到该相机外参。**RoboInter 给 mask,官方给外参,靠这个 key 缝起来。**

## R4. 怎么做 geometric-ambiguity 角度分桶(RH20T 独有实验)

1. 取一条 recording 里 **≥2 路 masked 全局相机**(R1);
2. 从官方 `calib/` 取每路 serial 的外参 → 相机在基座系的位姿;
3. 对每个 exo↔exo pair 算**相对视角夹角**(两相机光轴夹角,或相对物体的视线夹角);
4. **按夹角分桶**:如 30° 内 / 30–60° / 60–90° / >90°(对侧);
5. 每桶各跑 V²-SAM 的 exo↔exo,画 **IoU 随夹角退化曲线** → 把计划书 "A. Geometric ambiguity" 从定性变可控变量。

> 若只用社区 LeRobot 镜像(`hainh22/rh20t`,见主报告)**不带外参** → 要么走官方 `calib/`,要么用 MoGe-2 估计(计划书 §2.3 备注)。

## R5. wrist↔exo & caveats(RH20T 专属)

1. **腕部无 mask**:RoboInter 的 RH20T mask **全在全局相机 serial 上,无 in-hand mask**(同 DROID)→ wrist↔exo 需自建腕部真值(路线同 §6)。
2. **in-hand 相机识别**:`cam_{serial}` 不区分全局/腕部,要靠 `rh20t_api`/metadata 判定哪路是 in-hand。
3. **外参来源**:官方 `calib/` 有;社区 LeRobot 镜像不带(见 R4)。
4. **license**:优先 RH20T-C(scene 0001–0005,可商用)。
5. **分辨率**:RoboInter 视频 180×320;官方最高 1280×720。mask 具体分辨率下载后确认(结构同 DROID)。
6. **跨视角物体一致性**:同 DROID caveat ③,多路 serial 的 mask 是否咬同一物体需 spot-check(RH20T 多相似物体、视角差大,更要抽查)。

## R6. 一句话总结

- **想快**:走路线① —— RoboInter 现成 mask,RH20T 同一 recording 多路全局相机可两两组,exo↔exo 产能全场最高,代码同 DROID。
- **要卖点**:叠路线② —— 用官方 `calib/` 外参按夹角分桶,做 geometric-ambiguity 梯度曲线,这是 RH20T 相对 DROID 独有、也是 PAMI diagnostic bench 的差异化。
- **短板**:同 DROID,wrist↔exo 无现成 mask;另 in-hand 识别、外参来源、license 分段要注意。

---
---

# 三、RoboMIND(第三)—— 数据格式与用法

> 论文 <https://arxiv.org/abs/2412.13877> · HF `x-humanoid-robomind/RoboMIND`(gated)· ModelScope `X-Humanoid/RoboMIND`(免 gate)· 日期 2026-07-13,HDF5 结构本地实测。
> **RoboMIND 相对 DROID/RH20T 的独有价值**:**4 种 embodiment 同任务采集** → 能做 "object 在不同**机器人**" 的 cross-embodiment split(计划书 goal 的后半句)。**代价**:自带**无任何 mask**,exo/wrist 两侧都要从零自建。

## M0. 与 DROID/RH20T 的根本不同

- DROID/RH20T 有 **RoboInter 现成 mask**;**RoboMIND 完全没有 mask**(也没 bbox)→ 每一路都要 SAM2 自建 + 人工复核(工作量约 3–4 周,见主报告 §6)。
- 数据是**原始 HDF5**(每 trajectory 一个 `.h5`),不是 LeRobot parquet+mp4。

## M1. HDF5 格式(本地实测 `pick_apple_into_drawer` franka_3rgb)

```
trajectory.hdf5                        # attrs: compress=True, sim=False(无时间戳字段!)
├── observations/
│   ├── rgb_images/
│   │   ├── camera_left   (T,) object    # 每帧一段 JPEG 字节(magic ff d8)→ cv2.imdecode
│   │   ├── camera_top    (T,) object
│   │   └── camera_right  (T,) object
│   └── depth_images/                    # 深度(部分子集有)
├── master/joint_position  (T, 8)        # 遥操作 leader 臂关节
└── puppet/                              # follower(真实机器人)臂状态
    ├── joint_position     (T, 8)
    └── end_effector       (T, 6)
```

- **图像是逐帧 JPEG 字节**(不是原始数组),读法:
  ```python
  import h5py, cv2, numpy as np
  with h5py.File("trajectory.hdf5") as h:
      buf = h["observations"]["rgb_images"]["camera_top"][t]   # bytes
      img = cv2.imdecode(np.frombuffer(buf, np.uint8), cv2.IMREAD_COLOR)  # BGR
      # franka_3rgb 是 BGR,需 cv2.cvtColor(img, cv2.COLOR_BGR2RGB);agilex_3rgb 已是 RGB
  ```
- **无 timestamp / 无 mask / 无 bbox**;`master/puppet` 是遥操作 leader-follower 臂状态。

## M2. 相机配置(决定哪个子集可用)

| 子集 | 相机(HDF5 key) | 颜色 | 跨视角类型 | 可用性 |
|---|---|---|---|---|
| `h5_franka_3rgb` | `camera_top` / `camera_left` / `camera_right` | **BGR** | **exo↔exo**(3 固定外部) | ✅(675 条只有 left+right) |
| `h5_agilex_3rgb` | `camera_front` / `camera_left_wrist` / `camera_right_wrist` | RGB | **wrist↔exo + wrist↔wrist**(2 腕 + 1 前) | ✅(RoboMIND 内唯一带 wrist) |
| `h5_franka_1rgb` / `h5_ur_1rgb` / `h5_tienkung_*_1rgb` | 单 `camera_top` | — | — | ❌ 单视角 |

- **多视角可用规模**(实测采样估计,见主报告 §2.1):真实 ≈ **31–34k**(franka_3rgb 真实 ~22k + AgileX 10,269),另 sim_franka_3rgb 26,070 是仿真。
- AgileX 双腕部相机随左右臂**独立运动** → wrist↔wrist 是"双动"难配对;wrist↔front 更可控。

## M3. 获取(HF 下不了走 ModelScope)

- HF `x-humanoid-robomind/RoboMIND` **gated**(账号需接受条款)。
- **ModelScope 免 gate 可直接下**:`https://www.modelscope.cn/datasets/X-Humanoid/RoboMIND/resolve/master/<path>`,支持 range;按 task 打包 `benchmark1_0_compressed/h5_franka_3rgb/<task>.tar.gz(.part-xx)`。**体量巨大**:franka_3rgb 全量 2.4 TB。
- 本仓库 `RoboMIND/` 有一个 ModelScope example 样本(`pick_apple_into_drawer`)+ 检查脚本 `inspect_robomind_crossview.py`。

## M4. 怎么用(无 mask,全自建;主打 cross-embodiment)

1. **选子集**:exo↔exo 走 `h5_franka_3rgb`;wrist↔exo 走 `h5_agilex_3rgb`。
2. **抽帧解码**:按 M1 读 HDF5、JPEG 解码、franka 记得 BGR→RGB。
3. **自建 mask**(核心成本):用语言标注定位目标物体 → 各视角**独立** SAM2 视频传播 → 跨视角身份人工关联 → 人工复核边界(主报告 §6 流程)。
4. **构 query/target**:同 §5(query 1 帧 + mask → target 所有帧),写 V²-SAM json。
5. **cross-embodiment split**(RoboMIND 独有):同一任务在 Franka 与 AgileX 上分别采,query 与 target 来自**不同 embodiment 的同任务不同 take**,物体同类/同实例 → 定义为 benchmark 的 hard split(非同步 + 跨形态)。

## M5. caveats

1. **零 mask**:exo/wrist 两侧都要自建(不像 DROID/RH20T 有现成外部 mask)→ 成本最高的候选。
2. **无 timestamp**:释出 HDF5 只有 `master/observations/puppet`,**无时间戳字段**,per-embodiment fps 不可考(须原始 ROS bag)。
3. **颜色**:franka_3rgb 是 **BGR**,agilex_3rgb 是 RGB,别搞反。
4. **cross-embodiment 一致性**:跨机器人时 query/target 是**不同物理 take**,只能保证同任务/同类物体,不是同一时刻同一实例——这是它和 DROID/RH20T(同时刻同实例)的本质区别,写论文要说清。
5. **体量**:全量 TB 级;先用 ModelScope 按 task 抽小样做 pilot。

---
---

# 四、AIRoA MoMa —— 数据格式与用法(新 wrist↔exo,实测共视最好)

> 官网 <https://www.airoa.org/project/> · 论文 <https://arxiv.org/abs/2509.25032> · 官方 HF `airoa-org/airoa-moma`(gated)· 免 gate demo `airoa-org/corl2025_demo`(官方 org,全分辨率)· 日期 2026-07-13,格式本地实测。
> **独有价值**:head(外部)俯看工作台,**实测目标物体在 head↔hand 共视成立**(优于 Galaxea,见 `AIRoA_MoMa/`)→ 当下最值得跟进的**新 wrist↔exo 源**。**代价**:同 RoboMIND,自带无 mask。

## A0. 定位

- 结构上是干净的 **hand(腕部,鱼眼)+ head(外部,俯看工作台)双路同步**,和 DROID 的 wrist↔exo 同构,但**分辨率更高、head 构图更贴工作台**。
- **无 mask**(仅分层语言 + 相机内参)→ 两侧都要自建;但 query 侧(head)物体清晰,自建可行性好。

## A1. LeRobot v2.1 格式(本地实测 demo)

demo 按 **task 分文件夹**(001, 003, …034),每个 task 是一个自包含 LeRobot 数据集:

```
{task}/
├── data/chunk-000/episode_*.parquet       # 每帧 state/action(见下)
├── videos/chunk-000/
│   ├── observation.image.head/episode_*.mp4   # 外部,480×640 AV1 @10fps
│   └── observation.image.hand/episode_*.mp4   # 腕部鱼眼,同规格
└── meta/
    ├── info.json          # robot_type=hsr, fps=10, 特征定义
    ├── episodes.jsonl     # 每 episode: task 文本 + length + uuid + bag_path + **calib(相机内参!)**
    ├── tasks.jsonl        # task_index -> 文本
    └── episodes_stats.jsonl
```

- **parquet 逐帧列**(实测):`observation.state`(8 维关节)、`observation.wrist.wrench`(力/力矩)、`observation.end_effector_pose.absolute/relative`、`observation.head_pose.absolute`(**head 会动,逐帧位姿**)、`action.{absolute,relative,arm,gripper,head,base}`、`timestamp`、`frame_index`、`primitive_action_index` 等。
- **`episodes.jsonl` 里带 `calib`**:如 `/hsrb/hand_camera/camera_info` 的 `height/width/K`(相机内参)。→ 有内参,但 head 是移动头(靠 `head_pose.absolute` 逐帧外参),不是 RH20T 那种静态多机位基座系标定。
- **视频 AV1**:用 decord/PyAV 解码(cv2 可能解不了),本仓库 `crossview_viz.decode_video_frames_robust` 已封装。

## A2. 获取(官方 gated → 走 demo)

- 官方 `airoa-org/airoa-moma` **gated**(账号需接受条款,640×480@30fps)。
- **免 gate demo**:`airoa-org/corl2025_demo`(**同一官方 org、同一 HSR rig、全分辨率 480×640@10fps**)—— 34 个 task,每 task 几个 episode,**demo 本身就可能够抽一个 hundred-level pilot**,不必非要官方全量。本仓库 `AIRoA_MoMa/` 已存样本 + `inspect_airoa.py`。

## A3. 怎么用(无 mask,自建;wrist↔exo)

1. **抽帧**:decord 解 AV1(head + hand 逐帧同步,按 frame_index 对齐)。
2. **自建 mask**:head 侧物体清晰,先在 head 上定位+SAM2 传播得 query mask;hand 侧(鱼眼)**先去畸变**再 SAM2 传播 + 人工复核。
3. **构 query/target**:query = head 1 帧 + mask → target = hand 所有帧(或反向);写 V²-SAM json,同 §5。
4. **难例价值**:hand 鱼眼 + 尺度差 + 货架多相似瓶 → 对应任务非平凡,正好当 hard case。

## A4. caveats

1. **零 mask**:两侧自建(同 RoboMIND);但共视好、query 侧清晰,比 Galaxea 可行。
2. **hand 是鱼眼**:做 mask 对应前要去畸变(内参在 `episodes.jsonl` calib)。
3. **head 会动**:`head_pose.absolute` 逐帧变,不是固定第三人称;要几何对齐得用逐帧位姿,不像 RH20T 有静态基座系外参。
4. **demo vs 官方**:demo 是 10fps、官方 30fps;demo 全分辨率、官方 org 出品,做 pilot 足够,正式再申请官方 gate。
5. **共视仍需逐 task 抽查**:目前实测是 task 001/010 共视好,34 个 task 建议再抽几个确认"head 俯看工作台"普遍成立。
